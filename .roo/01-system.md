- Never read any files in `./data/datasets/` directly. They are large!
  Run head or tail commands to inpect them.
  If you think you must read them, ask first or make it a TODO.
- If you're looking for project context, read `./.roo_project.yml` first.